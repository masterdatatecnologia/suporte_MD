select codigo, nome from pessoa WHERE     NOT f_obtem_campo_valido_latin1(nome);
select codigo, nome from pessoa WHERE     NOT f_obtem_campo_valido_latin1(fantasia_codinome)
select p.codigo, ep.logradouro from endereco_pessoa ep
inner join pessoa p on p.id_pessoa = ep.id_pessoa 
WHERE     NOT f_obtem_campo_valido_latin1(logradouro);

select p.codigo, ep.logradouro from endereco_pessoa ep
inner join pessoa p on p.id_pessoa = ep.id_pessoa 
WHERE     NOT f_obtem_campo_valido_latin1(bairro